[project]
name = "deeptech-ma-momentum"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "polars",
    "scikit-learn",
    "hdbscan",
    "umap-learn",
]

[dependency-groups]
dev = [
    "jupyterlab",
    "ipykernel",
    "seaborn",
    "matplotlib",
]
